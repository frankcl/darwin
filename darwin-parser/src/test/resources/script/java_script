function parse(request) {
    var userDefinedMap = {};
    userDefinedMap.url = request.url;
    var childURL = createChildURL("http://www.sohu.com/", 3);
    var childURLs = new Array();
    childURLs[0] = childURL;
    return buildOK(undefined, childURLs, userDefinedMap);
}